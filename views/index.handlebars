<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao blogAPP</h1>
  <p class="lead"></p>Assuntos relacionados a tecnologia</p>
  <hr class="my-4">
  <p>Programação, desenvolvimento e muito mais...</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>
<hr>
<h2>Postagens Recentes:</h2>
{{#each postagens}}
    
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-primary btn-sm">Leia mais</button></a>
        <hr>
        <small>Caegoria:{{categoria.nome}}</small><br>
        <small>Data de Publicação:{{data}}</small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}